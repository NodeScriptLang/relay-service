# Relay Service

Authenticates incoming and outgoing requests for NodeScript library modules.

Allows sending HTTP requests from Node.js backend, bypasing browsers' SameOrigin policies and eliminating associated issues.
